[package]
name = "corporate-nayose"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6", features = ["derive"] }
polars = { version = "0.55", default-features = false, features = [
  "lazy",
  "csv",
  "strings",
  "parquet",
  "streaming",
  "dtype-date",
  "temporal",
] }
